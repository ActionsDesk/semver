export enum Inputs {
  Bump = 'bump',
  Prelabel = 'prelabel',
  InitialVersion = 'initial_version'
}

export enum Outputs {
  Release = 'release'
}

export enum Bumps {
  major = 'major',
  minor = 'minor',
  patch = 'patch',
  pre = 'pre',
  final = 'final'
}
