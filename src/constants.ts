export enum Inputs {
  Bump = 'bump',
  Pre = 'pre',
  InitialVersion = 'initial-version'
}

export enum Outputs {
  Release = 'release'
}

export enum Bumps {
  major = 'major',
  minor = 'minor', 
  patch = 'patch',
  final = 'final'
}
