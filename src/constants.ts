export enum Inputs {
  Bump = 'bump',
  Pre = 'pre',
  InitialVersion = 'initial_version'
}

export enum Outputs {
  Release = 'release'
}

export enum Bumps {
  major = 'major',
  minor = 'minor', 
  patch = 'patch',
  final = 'final'
}
